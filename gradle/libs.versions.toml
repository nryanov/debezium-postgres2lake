[versions]
debezium = "3.4.1.Final"
iceberg = "1.10.1"
# tests
testcontainers = "2.0.3"

[libraries]
debezium-server-bom = { group = "io.debezium", name = "debezium-server-bom", version.ref = "debezium" }
debezium-bom = { group = "io.debezium", name = "debezium-bom", version.ref = "debezium" }

debezium-server-dist = { group = "io.debezium", name = "debezium-server-dist", version.ref = "debezium" }

iceberg-bom = { group = "org.apache.iceberg", name = "iceberg-bom", version.ref = "iceberg" }

# tests
testcontainers-bom = { group = "org.testcontainers", name = "testcontainers-bom", version.ref = "testcontainers" }

[plugins]

